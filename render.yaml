services:
  - type: web
    name: football-prediction
    env: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: ./start.sh
    envVars:
      - key: DEBUG
        value: False
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.12.0

